version: '{build}'

branches:
  only:
    - master

image:
  - Ubuntu2004

install:
  - ps: 'dir env:'
  - ps: 'cd ~ && svn checkout -r 2974 https://svn.code.sf.net/p/tass64/code/trunk tass64-code'
  - ps: 'cd ~/tass64-code && make'

build_script:
  - ps: 'make VERBOSE=1 TASS=$HOME/tass64-code/64tass'
    
